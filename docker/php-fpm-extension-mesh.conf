; Dockware's FPM master does not inherit arbitrary Compose variables. These
; values are deliberately local fixture configuration, not production secrets.
env[EXTENSION_MESH_ALLOW_PRIVATE_NETWORKS] = 1
